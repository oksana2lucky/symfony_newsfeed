{% extends 'layout.html.twig' %}

{% block page_contents %}
    {% block page_title %}
        <h1>{{ title|upper }}</h1>
    {% endblock %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit"/>
    {{ form_end(form) }}
{% endblock %}